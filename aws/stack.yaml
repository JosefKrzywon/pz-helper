AWSTemplateFormatVersion: '2010-09-09'
Description: 'Zomboid Helper - Complete Stack (CloudFront + S3 + Lambda + DynamoDB)'

Parameters:
  BucketPrefix:
    Type: String
    Description: 'Prefix for resource names'
    Default: 'pz-helper'
    AllowedPattern: '[a-z0-9-]+'

  DomainName:
    Type: String
    Description: 'Custom domain (leave empty to use the CloudFront default URL)'
    Default: ''

  HostedZoneId:
    Type: String
    Description: 'Route53 Hosted Zone ID (only needed with a custom domain)'
    Default: ''

  CertificateArn:
    Type: String
    Description: 'ACM Certificate ARN in us-east-1 (only needed with a custom domain)'
    Default: ''

  AdminUsername:
    Type: String
    Description: 'Initial admin username'
    Default: 'admin'

  AdminPasswordHash:
    Type: String
    Description: 'Admin password hash (sha256:salt:hash)'
    NoEcho: true

  SessionSecret:
    Type: String
    Description: 'Secret for signing session cookies'
    NoEcho: true

  BudgetLimit:
    Type: Number
    Description: 'Monthly budget limit in USD (0 = disabled)'
    Default: 5
    MinValue: 0
    MaxValue: 100

  BudgetAlertEmail:
    Type: String
    Description: 'Email for budget alerts (empty = no email alerts)'
    Default: ''

  LambdaConcurrencyLimit:
    Type: Number
    Description: 'Max concurrent Lambda executions (rate limit)'
    Default: 5
    MinValue: 1
    MaxValue: 100

Conditions:
  UseCustomDomain: !Not [!Equals [!Ref DomainName, '']]
  BudgetEnabled: !Not [!Equals [!Ref BudgetLimit, 0]]
  EmailAlertEnabled: !And
    - !Condition BudgetEnabled
    - !Not [!Equals [!Ref BudgetAlertEmail, '']]

Resources:
  # ============================================================
  # DYNAMODB TABLES
  # ============================================================
  UsersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub '${BucketPrefix}-users'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: username
          AttributeType: S
      KeySchema:
        - AttributeName: username
          KeyType: HASH
      SSESpecification:
        SSEEnabled: true
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  ProgressTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub '${BucketPrefix}-progress'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: visibleId
          AttributeType: S
      KeySchema:
        - AttributeName: visibleId
          KeyType: HASH
      SSESpecification:
        SSEEnabled: true
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # ============================================================
  # S3 BUCKET (website content)
  # ============================================================
  WebsiteBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${BucketPrefix}-website-${AWS::AccountId}'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # ============================================================
  # LAMBDA FUNCTION (API)
  # ============================================================
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${BucketPrefix}-lambda-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:Scan
                Resource:
                  - !GetAtt UsersTable.Arn
                  - !GetAtt ProgressTable.Arn
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  ApiFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub '${BucketPrefix}-api'
      Description: 'Zomboid Helper API (Login, Sync, Admin)'
      Runtime: nodejs22.x
      Handler: index.handler
      Timeout: 10
      MemorySize: 128
      ReservedConcurrentExecutions: !Ref LambdaConcurrencyLimit
      Role: !GetAtt LambdaExecutionRole.Arn
      Environment:
        Variables:
          USERS_TABLE: !Ref UsersTable
          PROGRESS_TABLE: !Ref ProgressTable
          SESSION_SECRET: !Ref SessionSecret
          ADMIN_USERNAME: !Ref AdminUsername
          ADMIN_PASSWORD_HASH: !Ref AdminPasswordHash
      Code:
        S3Bucket: !Sub '${BucketPrefix}-artifacts-${AWS::AccountId}'
        S3Key: lambda/sync-function.zip
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # Lambda URL for CloudFront origin
  ApiFunctionUrl:
    Type: AWS::Lambda::Url
    Properties:
      AuthType: NONE
      TargetFunctionArn: !GetAtt ApiFunction.Arn

  ApiFunctionUrlPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiFunction
      Action: lambda:InvokeFunctionUrl
      Principal: '*'
      FunctionUrlAuthType: NONE

  # IMPORTANT: Lambda URLs require BOTH permissions to work!
  ApiFunctionInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiFunction
      Action: lambda:InvokeFunction
      Principal: '*'

  # ============================================================
  # CLOUDFRONT FUNCTION (Auth)
  # ============================================================
  AuthFunction:
    Type: AWS::CloudFront::Function
    Properties:
      Name: !Sub '${BucketPrefix}-auth'
      AutoPublish: true
      FunctionCode: !Sub |
        import crypto from 'crypto';

        var SESSION_SECRET = '${SessionSecret}';

        // Constant-time string compare to avoid timing side channels
        function constantTimeEquals(a, b) {
          if (a.length !== b.length) return false;
          var xor = 0;
          for (var i = 0; i < a.length; i++) {
            xor |= (a.charCodeAt(i) ^ b.charCodeAt(i));
          }
          return xor === 0;
        }

        // Verify the HMAC-signed session token (same scheme as the Lambda):
        //   token = base64(payloadJSON) + "." + base64url(HMAC_SHA256(payload))
        function verifyToken(token) {
          if (!token) return false;
          var parts = token.split('.');
          if (parts.length !== 2) return false;
          var data = parts[0];
          var signature = parts[1];

          var expected = crypto.createHmac('sha256', SESSION_SECRET).update(data).digest('base64url');
          if (!constantTimeEquals(signature, expected)) return false;

          try {
            var payload = JSON.parse(Buffer.from(data, 'base64').toString('utf-8'));
            if (payload.exp && payload.exp < Date.now()) return false;
            return true;
          } catch (e) {
            return false;
          }
        }

        function handler(event) {
          var request = event.request;
          var cookies = request.cookies || {};
          var uri = request.uri;

          // API requests go straight to the Lambda (it does its own auth)
          if (uri.startsWith('/api/')) {
            return request;
          }

          // The login page itself must always be reachable
          if (uri === '/login.html') {
            return request;
          }

          // Everything else needs a VALID (signed, non-expired) session cookie
          var sessionCookie = cookies['pz_session'];
          if (sessionCookie && verifyToken(sessionCookie.value)) {
            return request;
          }

          // No valid session -> redirect to login
          return {
            statusCode: 302,
            statusDescription: 'Found',
            headers: {
              'location': { value: '/login.html' }
            }
          };
        }
      FunctionConfig:
        Comment: 'Auth check for Zomboid Helper'
        Runtime: cloudfront-js-2.0

  # ============================================================
  # SECURITY HEADERS POLICY
  # ============================================================
  SecurityHeadersPolicy:
    Type: AWS::CloudFront::ResponseHeadersPolicy
    Properties:
      ResponseHeadersPolicyConfig:
        Name: !Sub '${BucketPrefix}-security-headers'
        Comment: 'Security headers for Zomboid Helper'
        SecurityHeadersConfig:
          ContentSecurityPolicy:
            ContentSecurityPolicy: "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; form-action 'self'"
            Override: true
          ContentTypeOptions:
            Override: true
          FrameOptions:
            FrameOption: DENY
            Override: true
          ReferrerPolicy:
            ReferrerPolicy: strict-origin-when-cross-origin
            Override: true
          StrictTransportSecurity:
            AccessControlMaxAgeSec: 31536000
            IncludeSubdomains: true
            Preload: false
            Override: true
          XSSProtection:
            ModeBlock: true
            Override: true
            Protection: true

  # ============================================================
  # CLOUDFRONT DISTRIBUTION
  # ============================================================
  CloudFrontOAC:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub '${BucketPrefix}-oac'
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        Comment: 'Zomboid Helper'
        DefaultRootObject: index.html
        Aliases: !If
          - UseCustomDomain
          - [!Ref DomainName]
          - !Ref AWS::NoValue
        ViewerCertificate: !If
          - UseCustomDomain
          - AcmCertificateArn: !Ref CertificateArn
            SslSupportMethod: sni-only
            MinimumProtocolVersion: TLSv1.2_2021
          - CloudFrontDefaultCertificate: true
        Origins:
          # S3 Origin for static files
          - Id: S3Origin
            DomainName: !GetAtt WebsiteBucket.RegionalDomainName
            OriginAccessControlId: !Ref CloudFrontOAC
            S3OriginConfig:
              OriginAccessIdentity: ''
          # Lambda Origin for API
          - Id: ApiOrigin
            DomainName: !Select [2, !Split ['/', !GetAtt ApiFunctionUrl.FunctionUrl]]
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
              HTTPSPort: 443
        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods: [GET, HEAD, OPTIONS]
          CachedMethods: [GET, HEAD]
          Compress: true
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
          ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
          FunctionAssociations:
            - EventType: viewer-request
              FunctionARN: !GetAtt AuthFunction.FunctionARN
        CacheBehaviors:
          # API requests go to Lambda
          - PathPattern: /api/*
            TargetOriginId: ApiOrigin
            ViewerProtocolPolicy: redirect-to-https
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, POST, PATCH, DELETE]
            CachedMethods: [GET, HEAD]
            CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad  # CachingDisabled
            OriginRequestPolicyId: b689b0a8-53d0-40ab-baf2-68738e2966ac  # AllViewerExceptHostHeader
            ResponseHeadersPolicyId: !Ref SecurityHeadersPolicy
        CustomErrorResponses:
          # Serve the public login page (never the protected app) on S3
          # 403/404 so missing objects can't leak the app content.
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /login.html
            ErrorCachingMinTTL: 10
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /login.html
            ErrorCachingMinTTL: 10
        PriceClass: PriceClass_100
        HttpVersion: http2and3
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  WebsiteBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref WebsiteBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: AllowCloudFrontOAC
            Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub '${WebsiteBucket.Arn}/*'
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub 'arn:aws:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}'

  # ============================================================
  # ROUTE53 DNS
  # ============================================================
  DNSRecord:
    Type: AWS::Route53::RecordSet
    Condition: UseCustomDomain
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Ref DomainName
      Type: A
      AliasTarget:
        DNSName: !GetAtt CloudFrontDistribution.DomainName
        HostedZoneId: Z2FDTNDATAQYW2

  DNSRecordIPv6:
    Type: AWS::Route53::RecordSet
    Condition: UseCustomDomain
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Ref DomainName
      Type: AAAA
      AliasTarget:
        DNSName: !GetAtt CloudFrontDistribution.DomainName
        HostedZoneId: Z2FDTNDATAQYW2

  # ============================================================
  # COST PROTECTION: Budget + Auto-Disable
  # ============================================================
  
  # SNS Topic for Budget Alerts
  BudgetAlertTopic:
    Type: AWS::SNS::Topic
    Condition: BudgetEnabled
    Properties:
      TopicName: !Sub '${BucketPrefix}-budget-alert'
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # Email subscription for alerts (optional)
  BudgetAlertEmailSubscription:
    Type: AWS::SNS::Subscription
    Condition: EmailAlertEnabled
    Properties:
      TopicArn: !Ref BudgetAlertTopic
      Protocol: email
      Endpoint: !Ref BudgetAlertEmail

  # Lambda subscription for auto-disable
  BudgetAlertLambdaSubscription:
    Type: AWS::SNS::Subscription
    Condition: BudgetEnabled
    Properties:
      TopicArn: !Ref BudgetAlertTopic
      Protocol: lambda
      Endpoint: !GetAtt BudgetAlertHandler.Arn

  # Permission for SNS to invoke Lambda
  BudgetAlertLambdaPermission:
    Type: AWS::Lambda::Permission
    Condition: BudgetEnabled
    Properties:
      FunctionName: !Ref BudgetAlertHandler
      Action: lambda:InvokeFunction
      Principal: sns.amazonaws.com
      SourceArn: !Ref BudgetAlertTopic

  # Lambda Role for Budget Alert Handler
  BudgetAlertHandlerRole:
    Type: AWS::IAM::Role
    Condition: BudgetEnabled
    Properties:
      RoleName: !Sub '${BucketPrefix}-budget-handler-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DisableCloudFront
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - cloudfront:GetDistributionConfig
                  - cloudfront:UpdateDistribution
                Resource: !Sub 'arn:aws:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}'
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # Lambda Function to disable CloudFront on budget alert
  BudgetAlertHandler:
    Type: AWS::Lambda::Function
    Condition: BudgetEnabled
    Properties:
      FunctionName: !Sub '${BucketPrefix}-budget-handler'
      Description: 'Disables CloudFront distribution when budget threshold is exceeded'
      Runtime: nodejs22.x
      Handler: index.handler
      Timeout: 30
      MemorySize: 128
      Role: !GetAtt BudgetAlertHandlerRole.Arn
      Environment:
        Variables:
          DISTRIBUTION_ID: !Ref CloudFrontDistribution
      Code:
        ZipFile: |
          const { CloudFrontClient, GetDistributionConfigCommand, UpdateDistributionCommand } = require('@aws-sdk/client-cloudfront');
          
          const client = new CloudFrontClient({});
          const DISTRIBUTION_ID = process.env.DISTRIBUTION_ID;
          
          exports.handler = async (event) => {
            console.log('Budget alert received:', JSON.stringify(event, null, 2));
            
            try {
              // Get current distribution config
              const getCmd = new GetDistributionConfigCommand({ Id: DISTRIBUTION_ID });
              const { DistributionConfig, ETag } = await client.send(getCmd);
              
              if (!DistributionConfig.Enabled) {
                console.log('Distribution already disabled');
                return { statusCode: 200, body: 'Already disabled' };
              }
              
              // Disable the distribution
              DistributionConfig.Enabled = false;
              DistributionConfig.Comment = `DISABLED BY BUDGET ALERT - ${new Date().toISOString()} - Original: ${DistributionConfig.Comment}`;
              
              const updateCmd = new UpdateDistributionCommand({
                Id: DISTRIBUTION_ID,
                DistributionConfig,
                IfMatch: ETag
              });
              
              await client.send(updateCmd);
              console.log('CloudFront distribution disabled successfully');
              
              return { statusCode: 200, body: 'Distribution disabled' };
            } catch (error) {
              console.error('Failed to disable distribution:', error);
              throw error;
            }
          };
      Tags:
        - Key: Project
          Value: !Ref BucketPrefix

  # AWS Budget with alert thresholds
  CostBudget:
    Type: AWS::Budgets::Budget
    Condition: BudgetEnabled
    Properties:
      Budget:
        BudgetName: !Sub '${BucketPrefix}-monthly-budget'
        BudgetType: COST
        TimeUnit: MONTHLY
        BudgetLimit:
          Amount: !Ref BudgetLimit
          Unit: USD
        CostFilters:
          TagKeyValue:
            - !Sub 'user:Project$${BucketPrefix}'
      NotificationsWithSubscribers:
        # Warning at 50%
        - Notification:
            NotificationType: ACTUAL
            ComparisonOperator: GREATER_THAN
            Threshold: 50
            ThresholdType: PERCENTAGE
          Subscribers:
            - SubscriptionType: SNS
              Address: !Ref BudgetAlertTopic
        # Warning at 80%
        - Notification:
            NotificationType: ACTUAL
            ComparisonOperator: GREATER_THAN
            Threshold: 80
            ThresholdType: PERCENTAGE
          Subscribers:
            - SubscriptionType: SNS
              Address: !Ref BudgetAlertTopic
        # CRITICAL at 100% - triggers auto-disable
        - Notification:
            NotificationType: ACTUAL
            ComparisonOperator: GREATER_THAN
            Threshold: 100
            ThresholdType: PERCENTAGE
          Subscribers:
            - SubscriptionType: SNS
              Address: !Ref BudgetAlertTopic

Outputs:
  WebsiteURL:
    Description: 'Public URL of the site'
    Value: !If
      - UseCustomDomain
      - !Sub 'https://${DomainName}'
      - !Sub 'https://${CloudFrontDistribution.DomainName}'

  CloudFrontDomainName:
    Description: 'CloudFront default domain (works with or without a custom domain)'
    Value: !GetAtt CloudFrontDistribution.DomainName

  WebsiteBucketName:
    Value: !Ref WebsiteBucket

  UsersTableName:
    Value: !Ref UsersTable

  ProgressTableName:
    Value: !Ref ProgressTable

  CloudFrontDistributionId:
    Value: !Ref CloudFrontDistribution

  BudgetAlertTopicArn:
    Condition: BudgetEnabled
    Value: !Ref BudgetAlertTopic
    Description: 'SNS Topic for budget alerts'

  LambdaConcurrencyLimit:
    Value: !Ref LambdaConcurrencyLimit
    Description: 'Max concurrent API Lambda executions'
