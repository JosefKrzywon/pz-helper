# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*

# Lambda build artifacts
aws/lambda/*.zip

# Local config (may contain secrets)
aws/deploy.local.sh
aws/config.sh

# Server data (user progress)
server/data/

# Node (if someone runs npm install for testing)
node_modules/

# Logs
*.log

# Terraform (if migrated later)
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Temp files
*.tmp
*.temp

# Internal specs / intermediate docs (keep local, not in Git)
docs/requirements-aws.md
